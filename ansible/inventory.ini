[webservers]
# IP is dynamically injected by the GitHub Actions pipeline (deploy.yml)
# after terraform output -raw public_ip_address
# Do NOT hardcode the IP here — it changes on every destroy/redeploy
${VM_PUBLIC_IP} ansible_user=azureuser ansible_password={{ vm_password }} ansible_ssh_common_args='-o StrictHostKeyChecking=no'